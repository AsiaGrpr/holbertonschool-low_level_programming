#include <stdio.h>
/**
 * main - print all digit numbers separated by a space ans coma
 *@void: Null value
 *
 * Return: Zero value
*/

int main(void)
{
	char i;

	for (i = '0' ; i <= '9' ; i++)
		putchar (i);
		putchar (,);
		putchar ( );

	putchar ('\n');

	return (0);
}
