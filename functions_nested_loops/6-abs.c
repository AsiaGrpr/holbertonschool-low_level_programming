#include "main.h"

/**
 *_abs - give the absolute number
 *@int: number to check
 *
 * Return: Null value
*/

int _abs(int)
{
	if (int > 0)
	{
		_putchar('%d', int);
		return (0);
	}
	else if (int == 0)
	{
		_putchar('0');
		return (0);
	}
	else
	{
		z = 0 - int
		_putchar('%d', z);
		return (0);
	}
}
