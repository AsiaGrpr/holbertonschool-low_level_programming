#include "main.h"

/**
 *_abs - give the absolute number
 *@n: number to check
 *
 * Return: Null value
*/

int _abs(int)
{
	int n;
	if (n > 0)
	{
		_putchar('%d', n);
		return (0);
	}
	else if (n == 0)
	{
		_putchar('0');
		return (0);
	}
	else
	{
		z = 0 - n
		_putchar('%d', z);
		return (0);
	}
}
